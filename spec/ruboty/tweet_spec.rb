require "spec_helper"

RSpec.describe Ruboty::Tweet do
  it "has a version number" do
    expect(Ruboty::Tweet::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
